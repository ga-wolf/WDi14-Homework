<h1>Hello! Create new obligation</h1>

<p>Go on please create something new. Phew. we got there. We finally have a page!
</p>

<%= form_for @obligation do |f| %>
<p>
    <%= f.label :Name_of_agreement %>
    <%= f.text_field :agreement_name %>
</p>

<p>
    <%= f.label :Date_of_agreement %>
    <%= f.text_field :agreement_date %>
</p>

<h2>What's tasks does it create for us</h2>

<p><%= f.label :task %>
    <%= f.select :task_ids, Task.all.map { |t| [ t.description, t.id ] },
                                  {}, multiple: true %>
</p>

<p>
  The task type not here?  Go here to add a new task <%= link_to "New task", new_task_path %>
</p>

<p><%= f.label :due_date %>

</p>

<h2>From what clause</h2>

<p><%= f.label :clause_name%></p>
<%= f.select :clause_ids, Clause.all.map { |c| [ c.name, c.id ] },
{}, multiple: true %>

</p>

<p><%= f.label :clause_type%></p>
  <%= f.select :clause_ids, Clause.all.map { |c| [ c.description, c.id ] },
  {}, multiple: true %>
</p>

<p>
<%= f.label :contracting_party %>
<%= f.select :entity_ids, Entity.all.map { |e| [ e.name, e.id ] },
{}, multiple: true %>
</p>

<%= f.submit "Really new obligation" %>

<% end %>
