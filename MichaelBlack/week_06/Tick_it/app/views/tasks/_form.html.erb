<%= form_for @task do |f| %>
  <p>
    <%= f.label :title %>
    <%= f.text_field :title, :required => true %>
  </p>
  <p>
    <%= f.label :description %>
  </p>
    <%= f.text_area :description, size: "24x6", :required => true %>
  <p>
    <!-- <% projects_arr = [] %>
    <% @current_user.projects.each do |p| %>
      <% x = [p.title, p.id] %>
      <% projects_arr.push x %>
    <% end %> -->
  </p>
    <%= f.number_field :project_id, :value => @cur_project.id  %>
  <p>
    <%= f.submit "Submit" %>
  </p>
<% end %>
