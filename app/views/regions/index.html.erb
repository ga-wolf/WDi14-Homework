
<%= link_to "New region", new_region_path %>
<hr>
<% @regions.each do |region|%>
<p>Name: <%= link_to " #{region.name} ", "/regions/#{region.id}" %></p>
<p>Address: <%= region.address %></p>
<p><%= region.description %></p>
<hr>
<% end %>
