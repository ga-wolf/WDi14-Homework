<h1>This page should show all the spots</h1>

<% @spots.each do |spot| %>

<p>Name: <%= link_to " #{spot.name} ", "/spots/#{spot.id}" %></p>
<p>Address: <%= spot.address %></p>
<p><%= spot.description %></p>

<% end %>
