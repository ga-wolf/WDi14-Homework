// Part 1 - Calculator
//
var squareNumber = function (x) {
  var num1 = x * x;
  console.log( " The result of squaring the number " + num1 + " equals " + num2 );
  return num2
};



var halfNumber = function (x) {
  var num1 = x/2 ;
  console.log( " Half of " + num1 + " equals " + num2 );
  return num1
};

var percentOf = function (x, y) {
  // var num1 = x;
  // var num2 = y;
  // var num3 = (x - y);
  // var num4 = (num3 / y) * 100;
 var result = x / y * 100
 var message = ( x + " is " + reult + "%" + " of " + y )
  console.log( message );
}


var areaOfCircle = function(x) {
  var radiusCircle = x;
  console.log(x * x * 3.1456)
}



// Part 2
// Take half of the number and store the result.
// Square the result of #1 and store that result.
// Calculate the area of a circle with the result of #2 as the radius.
// Calculate what percentage that area is of the squared result (#3).
